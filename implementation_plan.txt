# 系統架構與實作計畫 (Implementation Plan)

本計畫基於「晚餐煮什麼？」PRD 規劃系統的技術架構與實作方向。為了保持清晰，我們將核心邏輯（過濾、計算配對程度與排序）模組化，並將所有的例外與邊界情況嚴謹考量。

## 系統架構設計

### 1. 技術選型建議
- **後端 / 核心邏輯**：Python。Python 在處理陣列比對、字典對照集上具備極高效率，保證能達成 PRD 要求之「2 秒內返回」效能指標 (PRD 12.1)。
- **資料儲存與配置**：初期以 JSON 檔案作為本地資料庫。這也滿足 PRD (12.2) 要求的「常備調味料清單需可由後台配置」的彈性設計。
- **前端 / 介面**：需能動態呈現「缺 X 樣」、「無結果建議」與「容忍值 Slider」。

### 2. 資料模型與驗證層 (Data Models & Validation)
- **User (使用者)**：持有廚具清單、容忍值偏好。
- **Recipe (食譜)**：基本屬性與必要廚具清單。
- **Ingredient Dictionary (食材字典)**：處理同義詞與常備品。
- **Data Validator (資料驗證層)**：載入食譜時，若缺少「食材」或「廚具」等必要欄位，視為低可信度自動拋棄，避免進入推薦系統 (PRD 11.3)。

## 推薦前置過濾與排序流程 (核心演算法)

1. **零輸入防呆**：若使用者未輸入任何食材 (0 項)，系統直接中止流程並回傳防呆提示 (PRD 11.4)。
2. **資料標準化**：將輸入字串透過 `synonyms` 轉換為標準名稱 (PRD 11.2)。
3. **廚具硬性過濾 (Hard Filter)**：
   - **Bypass 邏輯**：若使用者「未設定任何廚具」，則暫停套用廚具過濾，並附加「建議補設定」的提示 (PRD 11.1)。
   - **過濾邏輯**：若已設定廚具，且 `recipe.required_appliances` 包含使用者沒有的廚具，直接排除 (PRD 7.5, AC3)。
4. **缺料與符合度計算**：
   - 「非常備所需食材」= `recipe.ingredients` - `standard_condiments` (PRD 7.2)
   - 「缺少食材」= 「非常備所需食材」- 「使用者現有食材」
   - 「符合度」= 命中數 / 非常備所需食材總數 (PRD 8.2)
5. **容忍值過濾**：若缺料數 > 使用者設定的 Slider 容忍值，直接排除 (PRD 7.4, AC2)。
6. **結果判定與排序**：
   - 若經過上述過濾後**無任何食譜**：進入無結果處理，回傳友善建議文案（如：放寬容忍值、新增食材） (PRD 8.3)。
   - 若有結果，依序按：1. 缺料數量(小至大)、2. 匹配數量(大至小)、3. 符合度(高至低) 排序 (PRD 7.3)。完美匹配(缺0樣)自然會排在第一位 (AC1)。

## User Review Required
> [!IMPORTANT]
> 邊界情境與防呆機制已全數補齊至核心演算法計畫中。確認無誤後，我們將可以直接進入 **EXECUTION (開發階段)**，開始撰寫程式碼！

## Verification Plan
針對所有 PRD 提到的邊界情況，我們將實作對應測試：
- **AC1-AC3 測試**：確保精準配對、Slider 與廚具絕對過濾功能生效。
- **例外/邊界測試**：資料不完整食譜自動拋棄測試、空廚具 Bypass 測試、輸入 0 項食材防呆測試、無結果引導文案測試。
