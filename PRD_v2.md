# PRD（實作對齊版）

歐冠新 611421220 / 胡家楹 611421216

## 產品名稱
晚餐煮什麼？（今晚，煮點什麼？）

## 文件版本
v2.0（依實際實作修正，對齊 commit 現況）

## 文件目的
本文件為 v1.0 PRD 的修正版，目的在於讓需求描述「完全對齊目前專案的實際實作」。
除保留原始「依食材符合度、缺料容忍值與可用廚具條件推薦可實作菜譜」之核心需求外，
亦補入實作過程新增的功能（LLM 動態擴充菜譜、輸入驗證、圖片配置、冰箱訊息、
廚藝教學／食材保鮮頁等），並修正原 PRD 與實作不符的條目（效能、廚具持久化模型）。

> 與 v1.0 的差異彙整見文末「附錄 A：變更紀錄」。

---

## 1. 背景與問題描述
對多數下班後疲憊的租屋族而言，回家煮飯最大的痛點不是「沒有食譜」，而是：

1. 不知道現有食材能做什麼。
2. 推薦結果常出現「看得到卻做不到」的菜譜。
3. 缺少某些食材、廚具不齊全時，推薦結果仍不夠貼近真實情境。
4. 使用者在疲憊狀態下，不想再花力氣判斷哪些能做、哪些不能做。

因此，本功能讓使用者只需輸入冰箱現有食材，系統就能：
* 自動忽略常備調味料的缺漏
* 依符合度排序推薦菜譜
* 允許使用者自行設定最多可容忍缺少幾樣食材
* 自動過濾使用者沒有的廚具所需菜譜
* 當資料庫既有菜譜不足時，依使用者食材即時生成貼合的新菜譜

最終降低晚餐決策成本，提升「今晚真的做得出來」的信任感。

---

## 2. 產品目標

### 2.1 產品目標
協助租屋族或小家庭使用現有食材快速找到可執行的菜譜，降低做菜門檻與採買負擔。

### 2.2 使用者價值
* 用最少腦力完成晚餐決策
* 避免被不切實際的推薦內容消耗心力
* 降低額外採買成本
* 提高剩餘食材利用率

### 2.3 成功指標（KPI）
* 食譜點擊率（CTR）
* 推薦後查看完整步驟率／下載菜單率
* 使用者平均搜尋到第一個可接受菜譜的時間
* 推薦結果中「可實作菜譜占比」
* LLM 擴充觸發後仍能補滿 MIN_RESULTS 的比率

---

## 3. 目標使用者

* 租屋族
* 下班後體力與腦力低落
* 冰箱食材有限
* 不想額外採買
* 廚房設備簡單，可能只有快煮鍋、電磁爐或微波爐
* 希望快速找到今晚真的能做的菜

---

## 4. 使用情境
使用者打開網頁（單頁應用，免登入）：
1. 在「手邊有什麼好料？」輸入現有食材（支援自動完成建議）
2. 於「今天的小心情」勾選自己擁有的廚具（可即時新增自訂廚具）
3. 調整「最多可以缺幾樣食材」按鈕（0～3）
4. 按「來抽今晚的菜單！」送出推薦請求
5. 系統回傳依符合度排序的菜譜清單，並清楚標示缺少食材
6. 若資料庫既有菜譜不足，系統即時以 LLM 依其食材生成新菜譜補滿
7. 點入任一菜譜卡片，於彈窗查看食材與步驟，可下載 PNG 菜單
8. 另可瀏覽「廚藝教學」「食材保鮮」兩個知識頁

> 註：本版本為匿名使用，廚具與容忍值於**每次送出時即時讀取表單**，
> 不綁定個人帳號、不跨工作階段持久化（見 §6.4、§13、§11.1）。

---

## 5. 功能範圍

### 5.1 本次版本範圍（In Scope）
* 使用者輸入現有食材（文字輸入 + 自動完成建議）
* 系統忽略常備調味料的缺漏
* 依符合度排序食譜
* 缺少食材容忍值選擇器（0～3，預設 1）
* 根據使用者勾選廚具過濾食譜
* 推薦結果顯示缺少的食材數量與名稱
* 無結果時的友善替代建議
* **LLM 動態擴充**：既有結果不足 MIN_RESULTS 時依食材生成新菜譜
* **輸入合理性驗證**：以 LLM 標示無法辨識的食材／廚具
* **菜譜配圖**：以 Unsplash 為菜譜抓取成品照
* **冰箱悄悄話**：以 LLM 生成一句貼心訊息
* **菜單下載**：將菜譜匯出為 PNG
* **知識頁**：廚藝教學（刀法／術語／火候）、食材保鮮

### 5.2 不在本次範圍（Out of Scope）
* 自動辨識冰箱照片中的食材
* 即時串接外送／電商採買
* 根據烹飪技巧程度調整食譜難度
* 根據卡路里、飲食限制、過敏原做進階推薦
* 動態替代食材建議
* 語音輸入食材
* 使用者帳號系統與個人化「我的廚房」持久化設定

---

## 6. 名詞定義

### 6.1 現有食材
使用者當下持有並輸入至系統中的食材，例如：雞蛋、高麗菜。

### 6.2 常備調味料
系統預設為日常廚房常見、無須納入缺料計算的基礎品項。
目前由 `data/config.json` 之 `standard_condiments` 管理，內容為：

> 鹽巴、鹽、醬油、沙拉油、油、糖、白砂糖、黑醋、白醋、烏醋、胡椒、胡椒粉、
> 黑胡椒、米酒、香油、太白粉、韓式辣醬、水

對外顯示時（`/api/config/staples`）會去重、合併同義（如「鹽巴→鹽」「白砂糖→糖」
「胡椒粉／黑胡椒→胡椒」）並隱藏過於通用的「水、油」。

### 6.3 缺少食材容忍值
使用者可設定最多可接受缺少的「非常備品」數量：
* 0：完全不能缺料（只看現在就能做的）
* 1：可接受少買 1 樣（預設值）
* 2：可接受少數彈性
* 3：可接受較多彈性

範圍固定為 **0～3**，預設 **1**。後端對超出範圍或非整數值會回傳 400 錯誤。

### 6.4 可用廚具
使用者於首頁「我家廚房有的小幫手」區塊以核取方塊勾選實際擁有的設備。
預設選項：快煮鍋、平底鍋、微波爐、瓦斯爐（明火瓦斯爐）；
另可透過輸入框新增自訂廚具（例如氣炸鍋、電鍋）。

> 本版本廚具設定**不持久化**：每次送出推薦時即時讀取目前勾選狀態，
> 不綁定帳號、重新整理後回到預設。

### 6.5 符合度
系統依使用者現有食材與食譜需求食材的重疊程度計算之排序分數（見 §8.2）。

---

## 7. 功能需求

### 7.1 食材輸入功能
**描述**：使用者可輸入目前持有的食材，作為食譜推薦依據。

**功能需求**
* 支援多食材輸入（以標籤形式呈現，可逐一移除）
* 輸入方式為文字輸入，並提供常見食材自動完成下拉建議（鍵盤上下鍵／滑鼠可選）
* 支援常見中文食材名稱比對
* 查詢前對輸入做標準化：Unicode NFKC（全形轉半形）、去除前後空白與所有空格、同義詞對應
* 送出時若輸入框仍有未加入文字，會自動補加入

**輸入範例**：雞蛋、高麗菜、豆腐

### 7.2 常備調味料排除機制
**描述**：系統在計算缺漏食材時，自動排除常備調味料，不視為缺料。

**規則**
* 若食譜所需食材屬於常備調味料清單，不計入缺少食材數量
* 常備調味料不影響排序中的「缺料數」判定，且不列入符合度分母
* UI 不將常備調味料列為「尚缺食材」

**範例**
使用者持有：雞蛋、高麗菜；食譜需求：雞蛋、高麗菜、鹽巴、醬油 → 視為完整匹配，不顯示缺料。

### 7.3 食譜符合度排序
**描述**：推薦結果依符合程度排序，最容易立刻完成者優先。

**過濾與排序原則**（實作對應）
1. 廚具硬過濾：所需廚具有任一不在使用者清單則排除（使用者未設廚具時略過此步）
2. 容忍值過濾：缺少非常備品數量 > 容忍值則排除
3. 至少命中 1 樣使用者食材（`match_count ≥ 1`），否則排除，避免推出與輸入無關的菜
4. 排序鍵（依序）：缺少食材數量**少**者優先 → 現有食材匹配數**多**者優先 → 符合度分數**高**者優先

**顯示規則**：每道食譜顯示符合狀態、缺少食材數量與名稱。

**UI 標籤示例**
* 可直接做（缺 0 樣，`tag_class = ready`）
* 還缺 1 樣: 豬肉片（`tag_class = almost`）
* 還缺 2 樣: 蔥花, 豆干（`tag_class = missing`）

### 7.4 缺少食材容忍值選擇器
**描述**：使用者調整願意額外補買的食材數量。

**功能需求**
* 預設值 **1**，範圍 **0～3**（以四個按鈕呈現，非連續滑桿）
* 系統依設定值過濾食譜；缺少非常備品數量大於容忍值則不顯示
* 後端驗證：非整數回傳「missing_tolerance 必須為整數」；超出 0～3 回傳「missing_tolerance 範圍須介於 0 至 3」，HTTP 400

**UI 文案**
* 標題：最多可以缺幾樣食材呢？
* 0：0 樣：只看現在就能做的
* 1：1 樣：願意補買 1 樣
* 2：2 樣：可接受少數彈性
* 3：3 樣以上：可接受較多彈性

### 7.5 廚具過濾機制
**描述**：顯示食譜前依使用者勾選廚具過濾不可製作的菜譜。

**功能需求**
* 使用者於首頁勾選持有廚具（可自訂新增）
* 每道食譜資料含 `required_appliances` 標籤
* 若食譜任一必要廚具不在使用者清單，該食譜不顯示
* 使用者未勾選任何廚具時，暫不套用廚具過濾（見 §11.1）

**範例**：使用者持有快煮鍋；食譜需烤箱 → 不顯示。

### 7.6 LLM 動態菜譜擴充（新增）
**描述**：當資料庫既有結果不足時，依使用者食材即時生成新菜譜補滿。

**功能需求**
* 門檻常數 `MIN_RESULTS = 3`：符合條件結果數 < 3 且輸入有效時觸發
* 若輸入含被判定為無效的食材／廚具（見 §7.7），跳過擴充以免浪費呼叫
* 第一階段：依食材生成最多 3 道（`generate_for_ingredients`）
* 仍不足時第二階段：再生成一次，並將生成菜譜的廚具限制在使用者持有廚具內，
  以確保能通過廚具過濾
* 新生成菜譜會寫入資料庫並嘗試配圖；當次推薦中允許其略過容忍值過濾（`always_include`），
  避免剛生成就被隱藏
* 內容限制：嚴禁生成任何菜名含「炒蛋」之菜譜（含番茄炒蛋等）
* 另有「推薦次數累積擴充」：單道菜被推薦達 `REC_TIMES_THRESHOLD = 3` 次時，
  背景以既有菜譜為樣本生成新菜譜並重置計數

### 7.7 輸入合理性驗證（新增）
**描述**：以 LLM 判斷輸入的食材／廚具是否為真實合理項目。

**功能需求**
* 對非空的食材／廚具呼叫 `validate_inputs`
* 回傳 `invalid_ingredients`、`invalid_appliances`，前端以提示區塊顯示
  「偵測到無法辨識的輸入」並請使用者修正
* 寬鬆對待台灣常見口語、簡稱、地方稱呼；非真實項目（龍肉、時光機）、亂碼、純標點視為無效
* 驗證失敗（例外）時採容錯：視為全部合法，不阻斷主流程

### 7.8 菜譜配圖（新增）
**描述**：為菜譜抓取成品照，提升辨識與信任感。

**功能需求**
* 以 Unsplash 搜尋；查詢字串優先用菜譜既有 `image_query`，否則以 LLM 將
  中文菜名批次翻為英文關鍵字（`generate_image_queries`）
* 推薦回傳前同步補抓缺圖菜譜並寫回資料庫；抓取結果（含查無圖）皆持久化以利後續重用
* 未設定 Unsplash 金鑰或抓取失敗時靜默略過，前端以占位圖呈現

### 7.9 冰箱悄悄話（新增）
**描述**：以「會說話的冰箱」口吻產生一句 30–60 字的貼心訊息。

**功能需求**
* 推薦有結果時呼叫 `generate_fridge_message`，傳入食材與推薦菜名
* 自然提及 1–2 樣食材或一道菜名，語氣溫柔俏皮、不鼓勵叫外送
* 失敗時回傳 None，前端不顯示該區塊（屬選配視覺糖）

### 7.10 菜單下載（新增）
**描述**：將單一菜譜匯出為 PNG 圖檔。

**功能需求**
* 於菜譜彈窗按「下載菜單」，前端以 Canvas 繪製菜名、時間、廚具、食材、步驟與頁尾
* 等待網頁字型載入完成後再繪製，確保字體一致；檔名以菜名清理非法字元後產生

### 7.11 知識頁（新增）
* **廚藝教學**：基礎刀法、常見烹飪術語、火候控制（前端靜態資料）
* **食材保鮮**：蔬菜、肉類海鮮、蛋豆乳製品、調味乾貨之保存天數與方法（前端靜態資料）
* 與首頁以前端 SPA 路由（`#home` / `#cooking` / `#storage`）切換

---

## 8. 業務規則與邏輯

### 8.1 推薦前置過濾流程
1. **輸入防呆**：食材為 0 項（或全空白）→ 回傳「請至少輸入一項食材」並中止（§11.4）
2. **參數驗證**：`missing_tolerance` 非整數或超出 0～3 → 回傳 400（§7.4）
3. **資料驗證**：載入食譜時過濾缺少必要欄位之不完整資料（§11.3）
4. **輸入合理性驗證**：標示無效食材／廚具，並作為是否觸發 LLM 擴充的閘門（§7.7）
5. **廚具過濾**：未設定則略過；有設定則篩除不符之食譜（§7.5、§11.1）
6. **缺料計算**：排除常備調味料後，計算每道食譜缺少的非常備食材數（§7.2）
7. **容忍值篩選**：過濾超出容忍值之食譜（§7.4）
8. **命中門檻**：排除完全未命中使用者食材之食譜（`match_count ≥ 1`）
9. **符合度運算與排序**：計算符合度並依排序鍵回傳（§7.3、§8.2）
10. **不足補強**：結果 < MIN_RESULTS 時觸發 LLM 擴充（§7.6）
11. **無結果處理**：仍無結果時回傳友善建議（§8.3）

> 效能備註：步驟 4、7.8、7.9 及步驟 10 的擴充皆為同步 LLM／外部呼叫，
> 觸發時回應時間會明顯增加（見 §12.1）。

### 8.2 符合度計算
**符合度 = 已持有且命中的非常備食材數 ÷ 食譜所需非常備食材總數**

* 常備調味料不列入分母
* 分母為 0 時符合度視為 1.0
* 完整命中者符合度 = 100%

**範例**
* 使用者：雞蛋、高麗菜；食譜 A：雞蛋、高麗菜 → 2/2 = 100%
* 食譜 B：雞蛋、高麗菜、豬肉片 → 2/3 ≈ 66.7%，缺 1 樣

### 8.3 無結果處理
當所有食譜都被過濾掉、且擴充亦未補足時，回傳 fallback：
* 訊息：「目前沒有完全符合的菜譜」
* 建議：
  * 試著放寬「缺少食材容忍值」
  * 輸入更多你擁有的食材
  * 更新你的廚房或取消部分不必要的廚具限制

---

## 9. 介面需求

### 9.1 首頁元件
1. 食材輸入區（含自動完成、標籤、常備調味料說明摺疊面板）
2. 缺少食材容忍值選擇器（0～3 按鈕）
3. 廚具勾選區（預設項目 + 自訂新增）
4. 推薦結果列表（含載入動畫）
5. 每道食譜的缺料狀態標籤
6. 冰箱悄悄話訊息區（選配）
7. 無結果 fallback 卡片
8. 菜譜詳情彈窗（食材、步驟、下載按鈕、圖片來源標註）

### 9.2 推薦卡片資訊
* 菜名
* 圖片（無圖時顯示占位）
* 狀態標籤（可直接做／還缺 N 樣）
* 所需時間（`cook_time`，可空）
* 所需廚具簡述
* 點擊提示（點擊查看做法）

### 9.3 文案原則
文案減輕疲勞感、口語化、避免技術術語；圖片來源依 Unsplash 規範標註攝影師與來源連結。

---

## 10. 驗收標準

### AC1：基於現有物資的精準配對與排序
**Given** 使用者輸入「雞蛋、高麗菜」，常備品為「鹽巴、醬油、沙拉油」
**When** 系統計算配對並回傳清單
**Then**
* 只需「雞蛋、高麗菜（及常備品）」的食譜排在最前面
* 需要額外食材且至少命中 1 樣的食譜排在其後
* 部分匹配食譜清楚顯示缺少食材數量與名稱
* 常備調味料不列入缺料顯示

### AC2：缺少食材容忍值生效
**Given** 容忍值設為 0
**When** 某食譜需雞蛋、高麗菜、蔥花（蔥花為缺少的非常備品）
**Then** 該食譜不得出現；清單僅保留缺 0 樣非常備食材的菜譜

### AC3：廚具限制過濾
**Given** 使用者僅勾選快煮鍋
**When** 系統準備顯示結果
**Then** 所有需烤箱、微波爐、明火瓦斯爐等未持有設備的食譜在底層先被排除，
清單不出現任何使用者無法執行的菜譜

> 上述三項及 §7.2、§8.2、§11.1、§11.4 等，皆有對應自動化測試
> （`tests/test_engine.py`，7 項全數通過）與 BDD 情境
> （`tests/features/recipe_recommendation.feature`）。

---

## 11. 邊界情境與例外處理

### 11.1 使用者未設定廚具
* 預設策略：暫不套用廚具過濾，回傳所有通過其他條件的食譜
* fallback 建議中提示可更新廚房以提升準確度
* 本版本不做強制 onboarding 引導

### 11.2 食材名稱同義詞
以 `data/config.json` 之 `synonyms` 標準化，例如：
甘藍／包心菜→高麗菜、青蔥／蔥→蔥花、蛋→雞蛋、豬肉→豬肉片、蕃茄→番茄 等。

### 11.3 食譜資料不完整
缺少 `recipe_id`、`name`、`ingredients`、`required_appliances` 任一必要欄位，
或 `ingredients`／`required_appliances` 非陣列者，於載入時即被 `DataValidator` 過濾，
不進入推薦結果。

### 11.4 使用者輸入 0 項食材
* 不顯示推薦結果
* 回傳錯誤「請至少輸入一項食材」（全空白字串亦視為 0 項）

### 11.5 外部服務不可用
* OpenAI 金鑰未設定：擴充／驗證／配圖／冰箱訊息相關功能靜默略過，核心推薦仍可運作
* Unsplash 金鑰未設定或抓取失敗：以占位圖呈現
* 任一非核心步驟例外皆採容錯，不中斷主推薦回應

---

## 12. 非功能需求

### 12.1 效能
* **純資料庫比對情境**（未觸發任何 LLM／外部呼叫）：推薦結果應於 2 秒內返回
* **觸發 LLM 流程時**（輸入驗證、動態擴充、配圖、冰箱訊息）：因屬同步外部呼叫，
  回應時間將明顯拉長，無 2 秒保證
* 引擎以 60 秒 TTL 快取（`CACHE_TTL_SECONDS`），資料變動時即時失效
* 後續最佳化方向：將非核心 LLM／外部呼叫改為非同步或前端二段式載入（見附錄 B）

### 12.2 可維護性
* 常備調味料與同義詞清單由 `data/config.json` 配置
* 廚具類型與食譜標籤可擴充
* LLM 模型、提示詞集中於 `engine/recipe_generator.py`

### 12.3 可解釋性
* 結果顯示缺料數量與名稱、符合狀態標籤
* 圖片來源依規範標註

---

## 13. 資料需求

**食譜資料（PostgreSQL `recipes` 表）**
* `recipe_id`（SERIAL 主鍵）
* `name`（唯一）
* `ingredients`（JSONB）
* `required_appliances`（JSONB）
* `steps`（JSONB，可空）
* `cook_time`（INTEGER，可空）
* `rec_times`（INTEGER，預設 0，供累積擴充用）
* `image_url`、`image_credit`（JSONB）、`image_query`（圖片相關，可空）
* `created_at`（時間戳）

常備品標記由 `config.json` 的 `standard_condiments` 於計算時判斷，不存於食譜列。

**使用者資料**
* 本版本為匿名，固定 `user_id = "anonymous"`
* 廚具清單與容忍值於每次請求由前端表單傳入，不持久化
* 未來擴充：使用者帳號、個人化「我的廚房」與常備品設定

---

## 14. 技術架構（補充）

* **後端**：Python + Flask（`app.py`），核心引擎於 `engine/`
  （`recommender`、`calculator`、`normalizer`、`validator`、`models`、`storage`、
  `recipe_generator`、`unsplash`）
* **資料庫**：PostgreSQL，透過 `psycopg`（連線字串取自 `POSTGRES_URL`／`DATABASE_URL`）
* **LLM**：OpenAI `gpt-4o-mini`（`OPENAI_API_KEY`）
* **圖庫**：Unsplash API
* **前端**：原生 HTML／CSS／JS（`templates/index.html`、`static/`），SPA 路由
* **部署**：Vercel（`vercel.json`、`api/index.py`）

**主要 API 端點**
* `GET /`：首頁
* `POST /recommend`：推薦主流程
* `GET /api/config/staples`：對外常備調味料清單
* `POST /appliance/add`：新增自訂廚具（回應即驗證）
* `POST /recipes/expand`：手動觸發菜譜擴充

---

## 15. 後續可擴充方向
1. 使用者帳號與持久化「我的廚房」設定
2. 將非核心 LLM／外部呼叫非同步化以滿足整體效能
3. 缺少食材一鍵購物清單
4. 飲食偏好／減脂／素食篩選
5. 推薦替代食材
6. 拍照辨識食材自動輸入
7. 「我今天只想用快煮鍋」一鍵模式

---

## 附錄 A：相對 v1.0 的變更紀錄

| 類別 | v1.0 內容 | v2.0 修正 | 原因 |
| --- | --- | --- | --- |
| 效能（§12.1） | 一律 2 秒內返回 | 區分「純比對 2 秒內」與「觸發 LLM 無保證」 | 實作於主流程同步呼叫 LLM／外部 API |
| 廚具模型（§6.4、§13） | 「我的廚房」頁面持久化設定 | 匿名、每次請求即時讀取表單、不持久化 | 實作未做帳號與持久化 |
| 容忍值（§7.4） | 以 Slider 描述 | 以 0～3 按鈕呈現，後端含 400 驗證 | 對齊實際 UI 與驗證邏輯 |
| 排序（§7.3） | 五級排序原則 | 新增「至少命中 1 樣食材」門檻並列出實際排序鍵 | 對齊 `recommender` 實作 |
| 新增功能（§7.6–7.11） | 無 | LLM 動態擴充、輸入驗證、Unsplash 配圖、冰箱悄悄話、PNG 下載、知識頁 | 補登實作已存在功能 |
| 資料需求（§13） | 概念欄位 | 對齊 PostgreSQL 實際 schema（含 `rec_times`、圖片欄位） | 對齊 `storage` 實作 |
| 技術架構（§14） | 無 | 補入後端／DB／LLM／圖庫／部署與 API 端點 | 文件完整性 |

## 附錄 B：已知限制
1. 觸發 LLM 流程時無法保證 §12.1 的 2 秒效能；建議後續非同步化。
2. 廚具與容忍值不跨工作階段保存；重新整理回到預設。
3. LLM 生成內容品質與正確性依模型輸出，已用「炒蛋」關鍵字與去重等後置過濾把關，但非保證。

## 附錄 C：User Story
> 下班後腦力與體力皆耗盡的租屋族歐同學，想在系統輸入冰箱僅存的食材後，獲得扣除
> 「常備調味料」並依「符合度」排序的菜譜推薦，能調整「最多容忍缺幾樣食材」且自動
> 過濾沒有的廚具；當既有菜譜不足時，系統還會依他的食材即時生成新菜譜補上，讓他能
> 最省力地利用剩餘物資變出一餐，避免看到「看得到卻做不到」的豪華食譜而心累，最終
> 奪回下班後的放鬆時光與守護乾癟的錢包。
