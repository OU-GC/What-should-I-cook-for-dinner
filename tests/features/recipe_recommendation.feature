# language: zh-TW
# encoding: utf-8
#
# Feature: 晚餐煮什麼？ — 菜譜推薦系統
# PRD v1.0 | 歐冠新 611421220 / 胡家楹 611421216
#
# BDD Scenarios 對應 PRD 章節：
#   AC1  § 10 驗收標準 1
#   AC2  § 10 驗收標準 2
#   AC3  § 10 驗收標準 3
#   §7.2 常備調味料排除
#   §7.3 符合度排序
#   §7.4 缺少食材容忍值
#   §7.5 廚具過濾
#   §8.2 符合度計算
#   §8.3 無結果友善提示
#   §11.1 廚具未設定
#   §11.3 食譜資料不完整
#   §11.4 零食材輸入防呆

Feature: 菜譜推薦
  作為一個下班後疲憊的租屋族
  我希望輸入冰箱現有食材後
  系統能推薦我今晚真的能做的菜

  Background:
    Given 系統常備調味料清單為「鹽巴、醬油、沙拉油」
    And 系統食譜資料庫包含以下食譜：
      | 菜名         | 所需食材                    | 所需廚具  |
      | 炒高麗菜     | 高麗菜, 鹽巴, 沙拉油         | 平底鍋    |
      | 番茄炒蛋     | 雞蛋, 番茄, 鹽巴, 沙拉油     | 平底鍋    |
      | 滷肉飯       | 豬肉片, 醬油, 米, 蔥花       | 快煮鍋    |
      | 烤雞腿       | 雞腿, 鹽巴, 醬油             | 烤箱      |
      | 蒸蛋         | 雞蛋, 鹽巴                  | 電鍋      |

  # ─────────────────────────────────────────────
  # AC1：基於現有物資的精準配對與排序 (§ 10)
  # ─────────────────────────────────────────────

  Scenario: AC1 - 完整匹配食譜排在最前面
    Given 使用者持有食材「雞蛋, 高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 推薦結果中「炒高麗菜」應排在「番茄炒蛋」之前
    And 「炒高麗菜」的狀態標籤應顯示「可直接做」

  Scenario: AC1 - 部分匹配食譜需顯示缺少食材
    Given 使用者持有食材「雞蛋, 高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 推薦結果中「番茄炒蛋」應顯示缺少 1 樣食材
    And 「番茄炒蛋」缺少的食材名稱應為「番茄」

  Scenario: AC1 - 常備調味料不得出現在缺料清單中
    Given 使用者持有食材「雞蛋, 高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 「炒高麗菜」的缺少食材清單中不包含「鹽巴」
    And 「炒高麗菜」的缺少食材清單中不包含「沙拉油」

  # ─────────────────────────────────────────────
  # AC2：缺少食材容忍值 Slider (§ 7.4, § 10)
  # ─────────────────────────────────────────────

  Scenario: AC2 - 容忍值為 0 時不顯示需缺料的食譜
    Given 使用者持有食材「雞蛋, 高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中不應包含「番茄炒蛋」

  Scenario: AC2 - 容忍值為 1 時顯示缺 1 樣的食譜
    Given 使用者持有食材「雞蛋, 高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 推薦結果中應包含「番茄炒蛋」

  Scenario: AC2 - 容忍值超出範圍（4）應返回錯誤
    Given 使用者持有食材「雞蛋」
    And 使用者設定缺少食材容忍值為 4
    When 使用者發出推薦請求
    Then 系統應返回錯誤訊息「missing_tolerance 範圍須介於 0 至 3」
    And HTTP 狀態碼應為 400

  Scenario: AC2 - 容忍值為非整數應返回錯誤
    Given 使用者持有食材「雞蛋」
    And 使用者設定缺少食材容忍值為「很多」
    When 使用者發出推薦請求
    Then 系統應返回錯誤訊息「missing_tolerance 必須為整數」
    And HTTP 狀態碼應為 400

  # ─────────────────────────────────────────────
  # AC3：廚具限制過濾 (§ 7.5, § 10)
  # ─────────────────────────────────────────────

  Scenario: AC3 - 不持有所需廚具的食譜不應出現
    Given 使用者持有食材「雞腿, 鹽巴」
    And 使用者設定廚具「快煮鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中不應包含「烤雞腿」

  Scenario: AC3 - 持有所需廚具的食譜應正常顯示
    Given 使用者持有食材「雞蛋, 番茄」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中應包含「番茄炒蛋」

  # ─────────────────────────────────────────────
  # § 7.2 常備調味料排除機制
  # ─────────────────────────────────────────────

  Scenario: 食譜只缺常備調味料時應視為完整匹配
    Given 使用者持有食材「高麗菜」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中應包含「炒高麗菜」
    And 「炒高麗菜」的狀態標籤應顯示「可直接做」

  # ─────────────────────────────────────────────
  # § 8.2 符合度計算
  # ─────────────────────────────────────────────

  Scenario: 符合度 100% 的食譜排序高於部分匹配
    Given 使用者持有食材「雞蛋, 番茄」
    And 使用者設定廚具「平底鍋, 電鍋」
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 「番茄炒蛋」（符合度 100%）應排在「蒸蛋」（缺番茄 0 樣但分數較低）之前

  # ─────────────────────────────────────────────
  # § 8.3 無結果友善提示
  # ─────────────────────────────────────────────

  Scenario: 無符合食譜時應返回友善提示而非空白
    Given 使用者持有食材「蘿蔔」
    And 使用者設定廚具「快煮鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果清單應為空
    And 回應應包含 fallback 訊息「目前沒有完全符合的菜譜」
    And fallback 建議中應包含「試著放寬「缺少食材容忍值」」

  # ─────────────────────────────────────────────
  # § 11.1 廚具未設定
  # ─────────────────────────────────────────────

  Scenario: 使用者未設定廚具時不應套用廚具過濾
    Given 使用者持有食材「雞腿」
    And 使用者未設定任何廚具
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中應包含「烤雞腿」

  # ─────────────────────────────────────────────
  # § 11.3 食譜資料不完整
  # ─────────────────────────────────────────────

  Scenario: 缺少必要欄位的食譜不應進入推薦結果
    Given 系統食譜資料庫中有一筆食譜缺少「required_appliances」欄位
    And 使用者持有食材「雞蛋」
    And 使用者設定缺少食材容忍值為 3
    When 使用者發出推薦請求
    Then 該筆不完整食譜不應出現在推薦結果中

  # ─────────────────────────────────────────────
  # § 11.4 零食材輸入防呆
  # ─────────────────────────────────────────────

  Scenario: 使用者輸入 0 項食材時應顯示錯誤提示
    Given 使用者未輸入任何食材
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 系統應返回錯誤訊息「請至少輸入一項食材」
    And 推薦結果清單應為空

  Scenario: 使用者只輸入空白字串時也應視為零食材
    Given 使用者輸入食材清單為「"  ", ""」
    And 使用者設定缺少食材容忍值為 1
    When 使用者發出推薦請求
    Then 系統應返回錯誤訊息「請至少輸入一項食材」

  # ─────────────────────────────────────────────
  # § 7.1 食材名稱同義詞標準化 (§ 11.2)
  # ─────────────────────────────────────────────

  Scenario: 使用同義詞輸入食材仍能正確比對
    Given 系統同義詞字典中「甘藍」對應「高麗菜」
    And 使用者持有食材「甘藍」
    And 使用者設定廚具「平底鍋」
    And 使用者設定缺少食材容忍值為 0
    When 使用者發出推薦請求
    Then 推薦結果中應包含「炒高麗菜」
